#include <cuda_runtime.h>

// Note: input_a and output_c are all device pointers to float arrays
extern "C" void solution(float* input_a, float input_scalar, float* output_c, size_t n, size_t m) {
    
} 